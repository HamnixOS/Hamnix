# M14.2: kretprobe catches every openat() return value. The /init
# flow has several successful opens (cat /proc/*) and likely a few
# failures (probe for non-existent files); we just verify the hook
# fires AND register/unregister cycle is clean.
[KRET] register rc = 0
[KRET] unregistered
