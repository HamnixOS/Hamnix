# M2.4/M2.5: the [P] marker prepended by pynux_console_write proves the
# Pynux-registered console handled each printk. /proc/consoles must list
# `pynux0` (E=enabled), and the kernel logs the console becoming active.
[M2-CONSOLE] registered
[P]
printk: legacy console [pynux0] enabled
pynux0
[M2-CONSOLE] unregistered
